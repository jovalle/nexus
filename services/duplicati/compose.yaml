networks:
  nexus:
    external: true
services:
  duplicati:
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - SETGID
      - SETUID
    cap_drop:
      - ALL
    container_name: duplicati
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M
    environment:
      DUPLICATI__WEBSERVICE_PASSWORD: ${DUPLICATI_WEB_PASSWORD:?DUPLICATI_WEB_PASSWORD not set}
      PGID: ${PGID:?PGID not set}
      PUID: ${PUID:?PUID not set}
      SETTINGS_ENCRYPTION_KEY: ${DUPLICATI_ENCRYPTION_KEY:?DUPLICATI_ENCRYPTION_KEY not set}
      TZ: ${TZ:?TZ not set}
    healthcheck:
      interval: 30s
      retries: 3
      start_period: 60s
      test: [ "CMD-SHELL", "wget -q --spider http://localhost:8200 || exit 1" ]
      timeout: 10s
    hostname: duplicati
    image: lscr.io/linuxserver/duplicati
    labels:
      homepage.description: Backup Solution
      homepage.group: Utilities
      homepage.href: https://duplicati.${DOMAIN:?DOMAIN not set}
      homepage.icon: duplicati
      homepage.name: Duplicati
    networks:
      - nexus
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=256m
    volumes:
      - ./config:/config
      - ./source:/source:ro
      - ${BACKUP_PATH:?BACKUP_PATH not set}:/backups
