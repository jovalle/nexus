services:
  dockpeek:
    image: ghcr.io/dockpeek/dockpeek
    container_name: dockpeek
    hostname: dockpeek
    # Requires docker.sock read access
    environment:
      USERNAME: ${DOCKPEEK_USERNAME:?DOCKPEEK_USERNAME not set}
      PASSWORD: ${DOCKPEEK_PASSWORD:?DOCKPEEK_PASSWORD not set}
      SECRET_KEY: ${DOCKPEEK_SECRET_KEY:?DOCKPEEK_SECRET_KEY not set}
      TRAEFIK_LABELS: "true"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    labels:
      homepage.group: Dashboard
      homepage.name: Dockpeek
      homepage.icon: docker
      homepage.description: Docker Container Monitor
      homepage.href: https://dockpeek.${DOMAIN:?DOMAIN not set}
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M
        reservations:
          cpus: '0.05'
          memory: 32M
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - nexus
networks:
  nexus:
    external: true
