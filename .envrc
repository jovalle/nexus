# Nexus direnv configuration
# This file automatically configures the environment when entering the directory

# Add .bin directory to PATH for nx binary
PATH_add .bin

# Load environment variables from .env if it exists
dotenv_if_exists

# Notify user that nx is now available
echo "Nexus environment loaded. Use 'nx help' to get started."
